/**
 * App Component
 * 
 * Note: This file is no longer the main entry point.
 * Routing is now handled by AppRouter in src/core/router/AppRouter.tsx
 * 
 * This file is kept for potential future use (e.g., global providers, error boundaries)
 */

import './App.css';

function App() {
  return (
    <div>
      <p>
        If you see this, the routing is not configured correctly.
        Check main.tsx and AppRouter.tsx
      </p>
    </div>
  );
}

export default App;

